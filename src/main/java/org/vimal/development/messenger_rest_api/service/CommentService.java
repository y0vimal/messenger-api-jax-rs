package org.vimal.development.messenger_rest_api.service;

import java.util.List;

import org.hibernate.Query;
import org.hibernate.Session;
import org.vimal.development.messenger_rest_api.model.Comment;

public class CommentService {
	
	
	/*
	 * private Session session = null;
	 * 
	 * public List<Comment> getComments(){
	 * 
	 * session = DatabaseService.getSession(); session.beginTransaction(); Query
	 * query = session.createQuery("from Comment");
	 * 
	 * List comments = query.list();
	 * 
	 * return comments;
	 * 
	 * }
	 * 
	 * public Comment postComment(Long messageId, Comment comment) {
	 * 
	 * 
	 * 
	 * return null; }
	 */
	 
}
